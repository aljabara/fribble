-include "../util.sbl"
*	create subset of dictionary by copying only every 1000th entry
*	get words of length 5
	dict = getdict("../enable1.txt")			:f(end)
	dara = sort(dict,1)
	i = 0

loop
	word = dara[i = i + 50,1]				:f(end)
*	word = dara[i = i + 1,1]				:f(end)
*	ne(size(word),4)					:s(loop)
	output = differ(word) word				:(loop)
end
