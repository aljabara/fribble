

	define('validate(line)')				:(validate.end)
*	succeed if line contains only valid words, fail otherwise.	
validate
	line = line ' '
validate.1
	line span(' ') =
	ident(line)					:s(return)
	line break(' ') . word ' ' =
	valid(word)					:f(freturn)s(valid.1)
validate.end


				k
